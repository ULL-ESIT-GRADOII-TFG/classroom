# Start development mode executables.
# This assumes the postgresql is already running
elasticsearch: ./elasticsearch-1.7.6/bin/elasticsearch
memcached:     memcached
redis:         redis-server

web: ./bin/rails server -b 0.0.0.0 -p 8080
worker: ./bin/sidekiq -C config/sidekiq.yml
